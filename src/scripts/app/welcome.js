const textSample = "Zorg"

export function getWelcomeText(text = textSample){
  // Just a dummy function
  // Remove it and start creating your own module
  return `Welcome to ${text} 's boilerplate`
}

